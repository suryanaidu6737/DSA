class Solution {
    public int mySqrt(int n) {
        int l=0,r=n,mid=0;
        while(l<=r){
            mid=l+(r-l)/2;
            if(mid!=0 && mid==n/mid) return mid;
            else if(mid!=0 && mid>n/mid) r=mid-1;
            else l=mid+1;
        }
        return r;
    }
}
